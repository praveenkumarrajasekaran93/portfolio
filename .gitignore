# Aider & cache files
.aider*
*-tags-cache*

# Original source text (not deployed). The PDF IS deployed — it's offered
# as a download in the resume modal (see resume-templates.js 'original-pdf').
PRAVEENKUMAR_PR.txt

# Track only portfolio files
!index.html
!styles.css
!script.js
!profile-data.js
!resume-templates.js

# Resume PDF engine assets (must deploy so downloads work live)
!resume-pdf.js
!vendor/
!vendor/**
